import pandas as pd
import numpy as np
data = pd.DataFrame(np.arange(16.).reshape((4,4)),
                    index = ['上海', '北京', '蚌埠', '天津'],
                    columns = ['one', 'two', 'three', 'four'])
data
